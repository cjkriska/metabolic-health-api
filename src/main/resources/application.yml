spring:
  application:
    name: metabolic-health-api

  datasource:
    url: jdbc:mysql://localhost:3306/metabolic_health?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: charleskriska
    password: demopassword123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect